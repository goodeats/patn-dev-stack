#!/usr/bin/env tsx

/**
 * Epic Stack Updater - Simple Wrapper Script
 *
 * This is a simple wrapper that imports and runs the main Epic Stack updater.
 * It maintains the same interface as the original script while using the new
 * modular library structure.
 */

import './epic-stack-updater/index.js'
